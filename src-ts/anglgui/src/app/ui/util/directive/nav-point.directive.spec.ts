import { NavPointDirective } from './nav-point.directive';

describe('NavPointDirective', () => {
  it('should create an instance', () => {
    const directive = new NavPointDirective();
    expect(directive).toBeTruthy();
  });
});
