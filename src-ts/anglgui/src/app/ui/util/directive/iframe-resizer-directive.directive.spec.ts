import { IFrameResizerDirective } from './iframe-resizer-directive.directive';

describe('IFrameResizerDirectiveDirective', () => {
	it('should create an instance', () => {
		const directive = new IFrameResizerDirective();
		expect(directive).toBeTruthy();
	});
});
