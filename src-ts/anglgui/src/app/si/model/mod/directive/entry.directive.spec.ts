import { EntryDirective } from './entry.directive';

describe('EntryDirective', () => {
  it('should create an instance', () => {
    const directive = new EntryDirective();
    expect(directive).toBeTruthy();
  });
});
