
import { ViewContainerRef, ComponentFactoryResolver, ComponentRef } from "@angular/core";

export interface SiZone {
 
	
	initComponent(viewContainerRef: ViewContainerRef, componentFactoryResolver: ComponentFactoryResolver);
}
